prefix = /usr/local
CXXFILES = hithere.c
CXXFLAGS = -Wall -o hithere
LIBS = $(pkg-config --cflags --libs libftdi1) 

all:
	$(CC) $(CXXFILES) $(LIBS) $(CXXFLAGS) 

install:
	install hithere $(DESTDIR)$(prefix)/bin
	install hithere.1 $(DESTDIR)$(prefix)/share/man/man1

clean:
	rm -f hithere
